# Tienda Backend con Docker

Backend FastAPI con PostgreSQL usando Docker para aplicación de inventario y compras.

## Requisitos
- Docker Desktop

## Ejecutar localmente
```bash
docker-compose up --build